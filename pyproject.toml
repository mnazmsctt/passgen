[project]
name = "passgen"
version = "0.2.0"
description = "Password Generator CLI yang keren dan modern"
authors = [{ name = "nazmifirdaus", email = "nazmifurdaus@gmail.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "typer[all]>=0.9.0",
    "pyperclip>=1.8.0",
    "rich>=13.0.0",          # Tambahkan ini
]